{
  "name": "swimlac",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Lakeside Aquatic Club — swimlac.org",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify": "node scripts/verify.mjs",
    "ci": "npm run build && npm run verify",
    "deploy:hook": "node scripts/trigger-deploy.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.8"
  }
}
